require 'spec_helper'

describe ServiceLink do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ServiceLink.create!(@valid_attributes)
  end
end
